# Lox programming language
This is a lox programming language implementation written in C.

# Run the interpreter
```cd src```
```make build```

**run the repl**  
```./clox.o```
**execute the file**  
```./clox.o test.lox```
